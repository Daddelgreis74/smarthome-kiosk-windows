<Window x:Class="SmartHomeKiosk.SetupWizardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SmartHome Kiosk – Ersteinrichtung"
        Icon="app.ico"
        Width="640"
        SizeToContent="Height"
        MinHeight="620"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{StaticResource BgDark}"
        Foreground="{StaticResource TextPrimary}"
        Cursor="Arrow"
        FontFamily="Segoe UI">
    <Border Padding="28" Background="{StaticResource SurfaceDark}" Margin="12" CornerRadius="12" BorderBrush="{StaticResource BorderDark}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="SmartHome Kiosk – Setup Assistent" FontSize="22" FontWeight="Bold" Foreground="{StaticResource AccentCyan}"/>
                <TextBlock Text="Willkommen! Bitte richte deine Verbindung zum Dashboard und deine Kiosk-Präferenzen ein." 
                           Foreground="{StaticResource TextSecondary}" FontSize="13" Margin="0,4,0,0" TextWrapping="Wrap"/>
            </StackPanel>

            <!-- Content Fields -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <!-- Dashboard URL -->
                    <StackPanel>
                        <TextBlock Text="Dashboard URL:" FontWeight="SemiBold" FontSize="14" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,6"/>
                        <Grid>
                            <TextBox x:Name="TxtUrl" Style="{StaticResource ModernTextBox}" TextChanged="TxtUrl_TextChanged" Text=""/>
                            <TextBlock x:Name="TxtUrlPlaceholder" Text="https://192.168.178.100:8443/  (oder z. B. http://homeassistant.local:8123)" 
                                       Foreground="#64748B" FontSize="13" Margin="14,0,0,0" IsHitTestVisible="False"
                                       VerticalAlignment="Center"/>
                        </Grid>
                        <TextBlock Text="Gib hier die vollständige Web-Adresse deines SmartHome-Dashboards ein." FontSize="11" Foreground="{StaticResource TextSecondary}" Margin="2,4,0,0"/>
                    </StackPanel>

                    <!-- PIN Code -->
                    <StackPanel Margin="0,12,0,0">
                        <TextBlock Text="Kiosk PIN-Code:" FontWeight="SemiBold" FontSize="14" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,6"/>
                        <TextBox x:Name="TxtPin" Style="{StaticResource ModernTextBox}" Text="1234" MaxLength="8" Width="160" HorizontalAlignment="Left"/>
                        <TextBlock Text="Schützt das Einstellungsmenü (Mausrad-Klick oder 3-fach-Klick / 3s Touch oben links)." FontSize="11" Foreground="{StaticResource TextSecondary}" Margin="2,4,0,0"/>
                    </StackPanel>

                    <!-- Standby / Dimming -->
                    <Grid Margin="0,12,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,8,0">
                            <TextBlock Text="Display-Schoner nach (Min):" FontWeight="SemiBold" FontSize="13" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,4"/>
                            <TextBox x:Name="TxtDimMinutes" Style="{StaticResource ModernTextBox}" Text="3" Width="100" HorizontalAlignment="Left"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="8,0,0,0">
                            <TextBlock Text="Hardware-Standby nach (Min):" FontWeight="SemiBold" FontSize="13" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,4"/>
                            <TextBox x:Name="TxtStandbyMinutes" Style="{StaticResource ModernTextBox}" Text="15" Width="100" HorizontalAlignment="Left"/>
                        </StackPanel>
                    </Grid>

                    <!-- Cursor & Autostart CheckBoxes -->
                    <CheckBox x:Name="ChkAutoHideCursor" Margin="0,14,0,0" IsChecked="True" Cursor="Hand">
                        <TextBlock Text="Mauszeiger bei Inaktivität ausblenden (Empfohlen für Touch)" Foreground="{StaticResource TextPrimary}" FontSize="13"/>
                    </CheckBox>

                    <CheckBox x:Name="ChkAutoStart" Margin="0,10,0,0" IsChecked="True" Cursor="Hand">
                        <TextBlock Text="Mit Windows automatisch starten (Empfohlen für Wand-Displays)" Foreground="{StaticResource TextPrimary}" FontSize="13"/>
                    </CheckBox>
                </StackPanel>
            </ScrollViewer>

            <!-- Actions Footer -->
            <Grid Grid.Row="2" Margin="0,20,0,0">
                <Button Content="Abbrechen" Style="{StaticResource SecondaryButton}" HorizontalAlignment="Left" Width="110" Click="BtnCancel_Click"/>
                <Button Content="Einrichtung abschließen &amp; Starten" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Right" Padding="20,10" Click="BtnSave_Click"/>
            </Grid>
        </Grid>
    </Border>
</Window>
